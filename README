example:
static helloworld(){
    print("hello world!")
}

helloworld()

static age = 25;

static name = "Delocxo"

static height = 5.1

static data(age, name, height){
    export "This is "+ name + " hes " + age + " and " + height + "in height!"
}

print(data(age, name, height))

static guests = ["Angela", "Tom", "Dug", "Mattew", "Mart", "Robbeca"]

print("Guest Attending The Party")
looper(guests = i){
    print(i)
}

looper(5 = i){
    print(i)
}
